# Database config (choose one approach):
# 1) Full connection URL
DATABASE_URL=postgresql://localhost:5432/grad_cafe

# 2) Split connection fields
DB_HOST=localhost
DB_PORT=5432
DB_NAME=grad_cafe
DB_USER=grad_cafe_app
DB_PASSWORD=your_db_password

# Optional app/query settings
QUERY_LIMIT=100
STANDARDIZE_MAX_ROWS=100
STANDARDIZE_MAX_PROGRAM_CHARS=512
